#!/usr/bin/python

import setuptools
setuptools.setup(
    name='seco/range',
    version = '2.0',
    url='https://github.com/xadrnd/range',
    packages = setuptools.find_packages(),
    python_requires='>=3.6'
)

